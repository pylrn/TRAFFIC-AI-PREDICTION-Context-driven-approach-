# ── Web framework ─────────────────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
httpx>=0.27.0           # async HTTP client used by the orchestrator
python-dotenv>=1.0.0    # loads .env into os.environ

# ── Data / ML ─────────────────────────────────────────────────────────────
pydantic>=2.8.0
numpy>=1.26.0
scikit-learn>=1.5.0     # Random Forest model

# ── Deep learning (LSTM) ──────────────────────────────────────────────────
# CPU-only build is sufficient for inference; swap for the CUDA wheel if needed
#torch>=2.3.0

# ── Probabilistic graphical models (Bayesian Network) ─────────────────────
pgmpy>=0.1.25
